<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sketch_chain">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Sketch Chain"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".ui.gameplay.WaitingRoomActivity"></activity>
        <activity
            android:name=".ui.roomlist.RoomPWActivity"
            android:theme="@style/Theme.AppCompat.Light.Dialog"></activity>
        <activity android:name=".ui.makingroom.MakingRoomActivity" android:theme="@style/Theme.AppCompat.Light.Dialog"/>
        <activity android:name=".ui.roomlist.RoomListActivity" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".ui.gameplay.PlayingRoomActivity"/>
        <activity android:name=".ui.MainActivity" />
        <activity android:name=".ui.roomlist.SearchRoomActivity"/>
        <activity
            android:name=".ui.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>